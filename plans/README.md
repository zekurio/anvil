# Implementation Plans

Generated by the improve skill on 2026-07-02 at commit `4149ac0`. Execute in the order below unless dependencies say otherwise. Each executor: read the assigned plan fully before starting, honor its STOP conditions, run its verification gates, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-download-flow-safety.md) | Make download-library defaults use handoff, not replace | P1 | M | — | DONE |
| [002](002-nonresumable-checkpoint-errors.md) | Skip resume-checkpoint writes after non-resumable pipeline steps | P1 | M | — | DONE |
| [003](003-release-leased-jobs.md) | Release leased jobs when scheduler cancellation races with drain dispatch | P1 | S | — | DONE |
| [004](004-ignore-anvil-copy-outputs.md) | Ignore copy-mode Anvil outputs during scanning | P2 | M | — | DONE |
| [005](005-repair-mock-smoke-config.md) | Repair the mock smoke fixture configuration drift | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plans 001–005 are semantically independent and can be executed in parallel in separate worktrees.
- Merge-conflict caveat: Plans 001 and 005 can both touch `pkg/config/config_test.go` (005 only optionally), and Plans 001/004 both touch files under `cmd/anvild`-adjacent surfaces. Conflicts are textual, not semantic; rebase order does not matter.
- Plan 001 ↔ 005 interaction was checked at `4149ac0`: the mock fixture's download library already uses a handoff flow with no `media.replacement_mode`, so Plan 001's new validation does not break Plan 005's fixture. If both land, run `make mock-config-check` afterward.
- If a future CI plan is added, run Plan 005 first so CI has a valid mock fixture/config gate.
- Plan 001 touches config defaults and the NixOS module; run its Nix evaluation gate when Nix is available (see the corrected eval snippet in that plan — the module is a bare function and cannot be `nix eval --json`'d directly).
- Plan 004 adds a cross-package `scanner → replace` import (verified cycle-free at `4149ac0`); run `make build` as specified in that plan.

## Findings considered and not planned in this batch

- NixOS service runs as root by default: high-confidence hardening finding, but not in the selected top-five batch. Revisit with a dedicated NixOS service-hardening plan.
- Process cancellation may leave child encoders running: medium-confidence operational finding that needs process-group design and Linux-specific testing. Revisit separately.
- No CI workflow protects verification targets: defer until Plan 005 lands, then add CI around `make test`, lint, build, and the cheap mock config gate.
- Direction option — stale/missing media lifecycle: product/roadmap work, not a bug-fix plan in this batch.
- Direction option — configurable hard-fail validation: product policy decision; current README documents validation as observational.
- Direction option — Arr metadata caching/batching: performance/product trade-off; defer until real library scale makes it measurable.
